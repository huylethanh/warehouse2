import 'package:flutter/material.dart';

class OutboundScreen extends StatelessWidget {
  const OutboundScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(
      child: Text("Outbound goes here"),
    );
  }
}
