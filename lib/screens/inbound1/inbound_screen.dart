import 'package:flutter/material.dart';

class InboundScreen extends StatelessWidget {
  const InboundScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Placeholder(
      child: Text("Inbound goes here"),
    );
  }
}
