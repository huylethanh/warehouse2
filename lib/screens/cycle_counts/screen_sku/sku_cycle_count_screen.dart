import 'package:flutter/material.dart';

class SkuCycleCountScreen extends StatelessWidget {
  const SkuCycleCountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text("Kiểm kê theo SP"),
          centerTitle: true,
        ),
        body: const Placeholder());
  }
}
