import 'package:flutter/material.dart';

class SkuVerifyCycleCountScreen extends StatelessWidget {
  const SkuVerifyCycleCountScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text(
            "Xác nhận kiểm kê theo SP",
          ),
          centerTitle: true,
        ),
        body: const Placeholder());
  }
}
